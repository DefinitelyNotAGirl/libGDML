#include <libGDML.hxx>
extern "C" void gdml_member_open(std::string name, GDML_TYPE type)
{
    //this function gets called when a new member is added
}
extern "C" void gdml_member_close()
{
    //this function gets called when a new member is added
}